# Use a relative API path when the frontend and backend are served from the same domain
VITE_API_BASE_URL=/api/v1
VITE_GOOGLE_CLIENT_ID=

# If you deploy the API on a separate KuraMedics subdomain, switch to an absolute URL instead:
# VITE_API_BASE_URL=https://api.kuramedics.com/api/v1
